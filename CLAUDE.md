# Playground 작업 규칙

## 새로운 기능 개발

- 새로운 기능을 만들 때는 코드 수정 전에 반드시 해당 기능 전용 브랜치를 생성하고 전환한다.
- `main` 브랜치에서 새로운 기능을 직접 개발하지 않는다.
- 서로 다른 기능은 각각 별도의 브랜치에서 작업한다.
- 브랜치 이름은 `feat/<기능명>` 형식을 사용한다. 예: `feat/chat-streaming`.

```bash
git switch -c feat/<기능명>
```
